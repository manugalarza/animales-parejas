services:
  postgres:
    image: postgres:16
    container_name: animales-parejas-db
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER:     postgres
      POSTGRES_PASSWORD: secret
      POSTGRES_DB:       animalesParejasDB
    volumes:
      - ./postgres:/var/lib/postgresql/data